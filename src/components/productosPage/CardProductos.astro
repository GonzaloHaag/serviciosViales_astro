---
interface Props {
  imageUrl: string;
  title: string;
}
const { imageUrl, title } = Astro.props;
---


      
    <div
  class="grid grid-cols-1 md:grid-cols-2 gap-10 bg-transparent border-b border-solid border-slate-300 py-10"
>
  <img src={ imageUrl } class="w-full h-full rounded-xl" loading="lazy" />

  <div class="flex flex-col text-start justify-center gap-5 items-start">
    <h3 class="text-3xl text-sv-blue font-semibold">
      { title }
    </h3>
    <slot name="texto" />
    <a
      href="/contacto"
      title="Contáctanos"
      class="bg-black text-white text-base px-8 py-4 rounded-full transition-colors duration-300 hover:bg-sv-blue"
    >
      Contáctanos
    </a>
  </div>
</div>


